---
interface Props {
	title: string;
}
const { title } = Astro.props;
import FetchQueryInput from "../core/fetchQuery";
---

<section class="flex flex-col items-start gap-3">
	<h2 class="text-xl font-semibold">{title}</h2>
	<FetchQueryInput placeholder="Testing" client:load />
</section>
